# OC Papers - 엔지니어 보조 AI 시스템

> **사내망 논문 DB 기반 RAG 챗봇에서 엔지니어 보조 에이전트 AI로 진화**

**프로젝트 코드명**: OC Papers
**현재 버전**: v0.4.5 (2025-11-26)
**개발 기간**: 2024.10.14 - 현재
**상태**: ✅ Production Ready (49/49 테스트 통과)

---

## 🗣️ 중요: 대화 언어

**이 프로젝트의 모든 대화는 한글로 진행합니다.**

- **개발 논의**: 한글 사용 (기술 용어는 영어 병기 가능)
- **코드 주석**: 한글 권장 (복잡한 로직 설명)
- **문서화**: 한글 우선 (필요시 영문 버전 별도 작성)
- **커밋 메시지**: 한글 사용 가능
- **이슈/PR**: 한글로 작성

**예외**:
- 코드 내 변수명, 함수명, 클래스명: 영어 사용 (PEP 8)
- 기술 문서 일부: 국제 협업 필요시 영어 사용

---

## 📋 프로젝트 개요

### 🎯 핵심 목적

**OC Papers**는 사내망 환경에서 논문 데이터베이스를 구축하고, 이를 기반으로 엔지니어들에게 정확하고 신뢰할 수 있는 정보를 제공하는 AI 챗봇 시스템입니다. 향후 논문뿐만 아니라 다양한 개발결과물(엑셀, 파워포인트, PDF, raw data 등)을 활용하여 **엔지니어를 보조하는 자율 에이전트 AI**로 발전시킬 계획입니다.

### 🌟 차별화 요소

1. **폐쇄망 최적화**: 외부 네트워크 없이 완전히 작동 (로컬 모델 캐시)
2. **정확한 인용 시스템**: 95% 이상의 citation coverage (NotebookLM 스타일)
3. **지능형 검색**: 질문 유형 자동 분류 및 exhaustive retrieval
4. **Vision-Augmented**: PPTX 슬라이드 이미지 분석으로 표/그래프 인식
5. **Dual DB 아키텍처**: 개인 DB + 공유 DB 통합 검색

### 📊 운영 환경 및 규모

#### 개발 환경
- **개발 LLM**: Gemma3:4b (Ollama, 집 환경 - 속도 느림)
- **운영 LLM**: Llama-4-scout (Request API, 사내 환경)
- **임베딩**: mxbai-embed-large (Ollama)

#### 예상 데이터 규모
- **논문 수**: 수백 여건 (단계적 확대)
- **주요 분야**: OLED 소자, 광학, 재료과학
- **DB 용량**: TBD (논문당 평균 10-20MB 예상 → 총 5-10GB)

#### 사용자 규모
- **Phase 1 (네트워크 폴더)**: 약 20명
- **Phase 2 (DB 서버)**: 수백 명 예상
- **동시 접속**: 최대 10-20명 예상

#### 운영 방식
- **피드백 수집**: 수동 수집 (향후 시스템 통합 검토)
- **모니터링**: 성능 로그 자동 수집
- **배포 방식**: 수동 배포 (당분간)
- **업데이트 주기**: 월 1-2회 (기능 추가 시)

---

## 🏗️ 현재 아키텍처 (v0.4.0)

### 📊 시스템 구조

```
┌─────────────────────────────────────────────────────────────┐
│                    PySide6 Desktop UI                        │
│          (System Tray | Settings | Chat History)            │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                   RAG Chain (LCEL)                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Question   │→ │   Hybrid     │→ │  Re-ranker   │      │
│  │  Classifier  │  │   Retrieval  │  │(multilingual)│      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  Small-to-   │→ │   Citation   │→ │  Streaming   │      │
│  │    Large     │  │   Generator  │  │   Response   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                Vector Store Layer                            │
│  ┌──────────────────┐         ┌──────────────────┐          │
│  │  Personal DB     │ ◄─────► │   Shared DB      │          │
│  │  (ChromaDB)      │         │   (ChromaDB)     │          │
│  └──────────────────┘         └──────────────────┘          │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│               Document Processing Layer                      │
│  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐            │
│  │  PDF   │  │  PPTX  │  │  DOCX  │  │  XLSX  │            │
│  │ Parser │  │ Vision │  │ Parser │  │ Parser │            │
│  └────────┘  └────────┘  └────────┘  └────────┘            │
└─────────────────────────────────────────────────────────────┘
```

### 🔧 기술 스택

#### Core Framework
- **UI**: PySide6 (Qt6), Streamlit
- **RAG**: LangChain 0.3+ (LCEL)
- **Vector DB**: ChromaDB (persistent local storage)
- **Python**: 3.10+

#### LLM & Embedding
- **Ollama**: 로컬 LLM 서버 (gemma3:4b, llama3, etc.)
- **OpenAI**: API 지원 (gpt-4o, gpt-4o-mini)
- **범용 API**: vLLM, FastChat 등 OpenAI 호환
- **Embedding**: mxbai-embed-large (Ollama)

#### Document Processing
- **PDF**: PyMuPDF, pdfplumber, pypdf
- **PowerPoint**: python-pptx + Windows COM (Vision 렌더링)
- **Excel**: openpyxl
- **Vision**: Pillow, pywin32 (Windows)

#### Retrieval & Ranking
- **Vector Search**: ChromaDB (HNSW)
- **Keyword Search**: BM25 (rank-bm25)
- **Re-ranker**: sentence-transformers (cross-encoder/ms-marco-MiniLM-L-6-v2)
- **Hybrid**: RRF (Reciprocal Rank Fusion)

---

## 🚀 주요 기능 (v0.4.0)

### 1️⃣ SessionContext + Intent Detection (v0.4.0) ✅
- ✅ **SessionContext**: 5분 타임아웃 기반 최근 업로드 문서 자동 추적
- ✅ **IntentDetector**: 한국어/영어 문서 참조 패턴 감지 (지시대명사, 시간 기반, 파일명 추출)
- ✅ **5단계 검색 우선순위**: @멘션 → Intent (파일명) → Session (패턴) → Auto Session → Full DB
- ✅ **자연스러운 대화**: "이 문서", "방금 올린 파일", "this document" 등 표현 지원
- ✅ **GUI 자동 통합**: PDF/PPTX 업로드 시 자동 세션 추적

### 2️⃣ Phase B: 질문 분류 및 검색 최적화
- ✅ **Question Classifier**: 규칙 기반 + LLM 하이브리드 분류
  - 구체적 정보 (Specific Info)
  - 요약 (Summary)
  - 비교/대조 (Comparison)
  - 관계 분석 (Relationship)
- ✅ **적응형 Top-k**: 질문 유형별 최적 검색 개수 자동 결정
- ✅ **Exhaustive Retrieval**: "모든/전체" 키워드 감지 → 최대 100개 문서 검색

### 3️⃣ Phase C: Citation 95% 달성
- ✅ **다중 출처 지원**: 문장당 최대 2개 출처 인용
- ✅ **적응형 임계값**: 0.35-0.5 동적 조정
- ✅ **짧은 문장 최적화**: 10단어 이하 임계값 낮춤

### 4️⃣ Phase D: 답변 자연화
- ✅ **섹션 강제 구조 제거**: 자연스러운 답변 흐름
- ✅ **Inline Citation**: NotebookLM 스타일 인용 ([1], [2])
- ✅ **max_tokens 증가**: 4096으로 확대

### 5️⃣ Phase E: 품질 개선 (v3.5.0)
- ✅ **Score-based Filtering**: OpenAI 스타일 적응형 threshold
- ✅ **Re-ranker 통일**: multilingual-mini로 단일화
- ✅ **Hybrid Search 단순화**: 2단계 우선순위
- ✅ **Singleton 패턴**: Re-ranker 인스턴스 재사용

### 6️⃣ Vision-Augmented Chunking
- ✅ **PPTX 슬라이드 렌더링**: 표/그래프 자동 인식
- ✅ **Vision API 통합**: Ollama LLaVA, OpenAI Vision
- ✅ **일괄 처리**: PowerPoint 한 번만 열기

### 7️⃣ Small-to-Large 전략
- ✅ **구조 인식 청킹**: PDF/PPTX 구조 보존
- ✅ **컨텍스트 확장**: 800자 parent context
- ✅ **중복 제거**: 슬라이드/페이지 단위

---

## 📂 프로젝트 구조

```
RAG_for_OC_251014/
├── 📱 UI Layer
│   ├── desktop_app.py          # PySide6 데스크톱 앱 (메인)
│   ├── app.py                  # Streamlit 웹 앱 (선택)
│   └── ui/
│       ├── chat_widget.py      # 채팅 위젯
│       ├── settings_dialog.py  # 설정 다이얼로그
│       └── tray_icon.py        # 시스템 트레이
│
├── 🧠 RAG Core
│   └── utils/
│       ├── rag_chain.py        # RAG 체인 (LCEL)
│       ├── vector_store.py     # 벡터 스토어 관리
│       ├── document_processor.py # 문서 파싱
│       ├── reranker.py         # Re-ranker (cross-encoder)
│       ├── question_classifier.py # 질문 분류기
│       └── drive_scanner.py    # 공유 DB 스캐너
│
├── ⚙️ Configuration
│   ├── config.py               # 설정 관리자
│   ├── config.json             # 사용자 설정 (gitignore)
│   └── config.json.example     # 설정 템플릿
│
├── 🗄️ Database
│   ├── chroma_db/              # 개인 DB (로컬)
│   └── [shared_db]/            # 공유 DB (네트워크 드라이브)
│
├── 📦 Models (로컬 캐시)
│   ├── reranker-mini/          # Re-ranker 모델 (22MB)
│   └── tiktoken_cache/         # OpenAI tokenizer
│
├── 🧪 Tests
│   ├── test_phase2_verification.py  # Phase 2 검증
│   ├── test_phase2_integration.py   # Phase 2 통합
│   └── comprehensive_test.py        # 전체 시스템 테스트
│
├── 📚 Documentation
│   ├── README.md               # 사용자 가이드
│   ├── SYSTEM_QC_REPORT.md     # QC 보고서
│   ├── .CLAUDE.md              # 프로젝트 개요 (이 문서)
│   ├── RAG_Pipeline_Architecture.md  # RAG 파이프라인 아키텍처
│   └── docs/
│       ├── PHASE2_COMPLETION_SUMMARY.md
│       ├── QUESTION_CLASSIFIER_GUIDE.md
│       └── phase_d_implementation_plan.md
│
└── 🔧 Build & Deploy
    ├── RAG_System_v3.6.0.spec  # PyInstaller 스펙
    └── dist/RAG_System_v3.6.0/ # 빌드 결과물
```

---

## 🗺️ 향후 발전 방향 (로드맵)

### 🔧 Phase 2.5: PDF/PPTX 개선 및 시스템 최적화 (Phase 3 준비)
**목표**: 기존 문서 처리 품질 향상 및 운영 안정화
**예상 소요 시간**: 1.5-2주
**우선순위**: High

#### 2.5.1 Question Classifier 로깅 시스템
- [ ] **분류 기록 로깅**: JSONL 형식으로 모든 분류 기록 저장
  ```python
  # logs/classifier_history.jsonl
  {"timestamp": "2025-01-09T14:32:11", "query": "...", "type": "summary", "confidence": 0.85}
  ```
- [ ] **분석 도구 개발**: 로그 분석 스크립트 (타입별 분포, 낮은 신뢰도 케이스)
- [ ] **터미널 실시간 출력**: verbose 모드 시 분류 결과 출력
- [ ] **성능 지표 추적**: 평균 신뢰도, 오분류율 모니터링

**예상 소요 시간**: 1일

#### 2.5.2 PDF 그래프/표 Vision 처리
- [ ] **PyMuPDF 이미지 추출**: PDF 페이지별 이미지 자동 감지
- [ ] **GPT-4o Vision 분석**: 그래프/표 이미지 → 검색 가능한 텍스트 변환
  - 이미지 타입 식별 (그래프/표/도식)
  - 축 제목, 범례, 데이터 트렌드 추출
  - 수치 정보 텍스트화
- [ ] **오프라인 대안 검토**: Tesseract OCR + Camelot 표 추출 (비용 절감)
- [ ] **비용 최적화**: 이미지 크기 512x512 다운샘플링 (비용 50% 절감)
- [ ] **청킹 전략**: 이미지 설명을 해당 페이지 텍스트 청크와 병합

**예상 소요 시간**: 3-4일
**비용 추정**: 100페이지 논문 평균 5개 이미지 → $3.19 (512x512 기준)

#### 2.5.3 Exhaustive Retrieval 자동 감지
- [ ] **쿼리 복잡도 분석기**: LLM 기반 쿼리 범위 사전 판단
  - narrow (특정 정보) → top_k=3
  - medium (비교/관계) → top_k=10
  - broad (포괄 조사) → exhaustive retrieval (top_k=100)
- [ ] **Question Classifier 확장**: 기존 분류기에 `scope` 속성 추가
- [ ] **키워드 독립적 판단**: "모든/전체" 없이도 자동 감지
- [ ] **로깅 및 검증**: 자동 감지 정확도 추적

**예상 소요 시간**: 2-3일

#### 2.5.4 ChromaDB 동시 접속 대응
- [ ] **읽기 전용 공유 DB 모드**:
  ```python
  VectorStoreManager(mode="readonly")  # 공유 DB
  VectorStoreManager(mode="readwrite") # 개인 DB
  ```
- [ ] **파일 잠금 처리**: 쓰기 작업 시 `.lock` 파일 생성 (타임아웃 10초)
- [ ] **동시 접속 테스트**: 여러 사용자 시뮬레이션 테스트
- [ ] **운영 가이드 작성**: 공유 DB 업데이트 프로세스 문서화
  - 공유 DB: 관리자만 주기적 업데이트 (주 1회)
  - 개인 DB: 자유롭게 추가/삭제
  - 통합 검색: 중복 제거 로직 (공유 DB 우선)

**예상 소요 시간**: 1-2일

#### 2.5.5 성능 모니터링 및 로깅 시스템
- [ ] **응답 시간 측정 로깅**: 각 단계별 소요 시간 기록
  ```python
  # logs/performance_history.jsonl
  {
    "timestamp": "2025-01-09T14:32:11",
    "query": "...",
    "total_time_ms": 3500,
    "breakdown": {
      "retrieval_ms": 800,
      "reranking_ms": 1200,
      "llm_generation_ms": 1400,
      "context_expansion_ms": 100
    },
    "llm_model": "llama-4-scout",
    "num_docs_retrieved": 60,
    "num_docs_reranked": 60,
    "final_docs": 5
  }
  ```
- [ ] **병목 지점 자동 감지**: 3초 이상 소요 시 경고 로그
- [ ] **성능 대시보드**: 평균 응답 시간, 병목 단계 시각화 (선택)
- [ ] **메모리 사용량 추적**: Re-ranker, LLM 메모리 프로파일링

**예상 소요 시간**: 1일

#### 2.5.6 메타데이터 기반 검색 아키텍처 (Metadata-based Search)
**배경**: "김철수 저자의 논문 요약해줘" 같은 메타데이터 쿼리는 기존 의미론적 검색으로 부족
**목표**: 상용 서비스(Semantic Scholar, Perplexity) 수준의 메타데이터 검색 구현

- [ ] **PDF 메타데이터 자동 추출 파이프라인**:
  - GROBID 또는 PyPDF2 기반 메타데이터 추출
  - 저자명, 소속, 제목, 저널, 연도, DOI 자동 파싱
  - 첫 2페이지 정규식 매칭으로 저자/소속 추출 (GROBID 설치 어려운 경우)

- [ ] **ChromaDB 메타데이터 필드 확장**:
  ```python
  metadata = {
      "source": "paper.pdf",
      "page": 1,
      "authors": ["김철수", "이영희"],        # NEW
      "author_affiliations": ["LG Display"],  # NEW
      "title": "OLED 효율 향상 연구",         # NEW
      "journal": "Nature Photonics",          # NEW
      "year": 2024,                            # NEW
      "doi": "10.1038/...",                    # NEW
  }
  ```
  - DB 재구축 필요 (메타데이터 추가)
  - 기존 문서 재처리 스크립트 작성

- [ ] **Question Classifier 메타데이터 쿼리 타입 추가**:
  ```python
  # 새로운 쿼리 타입
  "metadata_search": {
      "description": "저자명, 제목, 기관 등으로 논문 검색",
      "keywords": ["저자", "작성자", "소속", "제목", "저널"],
      "examples": [
          "김철수가 쓴 논문 찾아줘",
          "LG Display 소속 연구자의 OLED 논문",
          "Nature에 실린 최신 연구"
      ]
  }
  ```

- [ ] **하이브리드 검색 전략 구현**:
  ```python
  # 2단계 검색
  if query_type == "metadata_search":
      # Stage 1: 메타데이터 필터링
      papers = vectorstore.get(
          where={"authors": {"$contains": "김철수"}}
      )

      # Stage 2: 필터링된 논문의 모든 청크 로드
      all_chunks = []
      for paper_id in papers:
          chunks = load_all_chunks_from_paper(paper_id)
          all_chunks.extend(chunks)

      # Stage 3: LLM에 전달하여 요약
      summary = llm.summarize(all_chunks)
  ```

- [ ] **상용 서비스 벤치마크 분석 문서 작성**:
  - `docs/METADATA_SEARCH_ANALYSIS.md` 생성
  - Semantic Scholar, Perplexity, Elicit 아키텍처 분석
  - Query Router, Fusion Ranking, Knowledge Graph 개념 정리
  - 현재 프로젝트 적용 방안 로드맵

**예상 소요 시간**: 3-5일
**우선순위**: Medium-High (사용자 요구사항 반영)

#### 2.5.7 사용자 피드백 시스템 (옵션)
- [ ] **답변 평가 UI**: 👍/👎 버튼 추가 (PySide6 ChatWidget)
- [ ] **피드백 로그 수집**:
  ```python
  # logs/user_feedback.jsonl
  {
    "timestamp": "2025-01-09T14:32:11",
    "query": "...",
    "answer": "...",
    "rating": "positive" | "negative",
    "user_comment": "...",
    "sources_used": [...]
  }
  ```
- [ ] **부정 피드백 알림**: 오답 케이스 자동 수집
- [ ] **주간 피드백 리포트**: 자동 생성 스크립트

**예상 소요 시간**: 2일 (선택 사항)

**Phase 2.5 완료 조건**:
- ✅ Question Classifier 로그 1주일 수집 완료
- ✅ PDF Vision 처리 샘플 테스트 (10개 논문)
- ✅ Exhaustive Retrieval 자동 감지 정확도 80% 이상
- ✅ ChromaDB 동시 접속 3명 이상 테스트 통과
- ✅ 성능 로깅 1주일 수집 완료
- ✅ 평균 응답 시간 5초 이하 달성 (Llama-4-scout 기준)
- ✅ 메타데이터 검색 기본 기능 구현 (저자명 검색)

---

### ✅ Phase 3: PDF Vision Hybrid Mode (완료 - 2025-01-20)
**목표**: PDF 표/그래프 Vision 처리 + 비용 최적화
**상태**: ✅ 핵심 기능 완료, 대규모 DB 최적화 남음

#### 3.1 PDF Vision Hybrid Chunking ✅
- ✅ **Smart Vision Decision**: 이미지/표 있는 페이지만 Vision API 사용
- ✅ **Poppler 자동 감지**: 번들된 Poppler 자동 탐지 및 사용
- ✅ **PDF → 이미지 변환**: pdf2image로 페이지별 PNG 생성
- ✅ **Vision API 통합**: OpenAI/Ollama Vision으로 표/그래프 분석
- ✅ **텍스트 폴백**: 텍스트 전용 페이지는 PyMuPDF 사용
- ✅ **비용 최적화**: 33-70% Vision API 호출 감소 (문서 종류에 따라)

**구현 파일**: `utils/pdf_chunking_engine.py` (lines 998-1252)
**완료일**: 2025-01-19

#### 3.2 Vector Storage & RAG Integration ✅
- ✅ **자동 임베딩 생성**: 청크 저장 시 자동 벡터화
- ✅ **메타데이터 보존**: page_number, chunk_type, document_id
- ✅ **Hybrid Search**: BM25 + Vector 통합 검색
- ✅ **Citation 생성**: Vision 청크도 정확한 페이지 인용

**테스트 완료**:
- `test_phase3_comprehensive.py` ✅
- `test_phase3_rag_FIXED.py` ✅
- `test_REAL_user_scenario.py` ✅ (격리 환경)

#### 3.3 검증 결과 (2025-01-20)

**✅ 격리 환경 테스트 (63개 문서)**:
```
✓ PDF 처리: 21개 청크 생성 (Vision 14 + Text 7)
✓ 벡터 저장: 21/21 성공
✓ 직접 검색: 우리 PDF가 상위 3개 결과에 포함
✓ RAG 쿼리: 우리 PDF의 3개 페이지(8, 10, 4)를 소스로 사용
✓ 최종 결과: ACTUALLY USEFUL [PASS]
```

**⚠️ 대규모 환경 문제 발견 (963개 문서)**:
```
✓ PDF 처리: 정상
✓ 벡터 저장: 정상
✗ 검색 우선순위: 신규 문서가 기존 문서들에 밀려 검색 안 됨
✗ RAG 쿼리: 다른 문서들로 답변 생성
```

**원인 분석**:
1. 신규 문서의 relevance score가 기존 문서들보다 낮음
2. 문서별 필터링 없이 전체 DB에서 검색
3. 최신 업로드 문서 우선순위 부스팅 없음

#### 3.4 회고 및 교훈 (2025-01-20)

**발견된 문제**:
- 이전 테스트들이 "작동"만 확인하고 "쓸모"는 검증 안 함
- 개별 컴포넌트 테스트만으로는 실제 유용성 보장 못 함
- 격리 환경과 프로덕션 환경의 괴리

**새로운 테스트 원칙 수립**:
1. End-to-end 사용자 시나리오 필수
2. 격리 환경 + 프로덕션 환경 모두 테스트
3. 실제 검색 가능성 직접 확인
4. "에러 없음" ≠ "성공"

**참고 문서**:
- `real_user_test_FINAL.txt`: 성공한 격리 환경 테스트
- `.CLAUDE.md` 테스트 철학 섹션 (lines 561-774)

---

### ✅ Phase 3.5: SessionContext + Intent Detection (완료 - 2025-11-20, v0.4.0)
**목표**: 자연스러운 문서 참조 및 최근 업로드 문서 우선 검색
**상태**: ✅ 완료 (49/49 테스트 통과)
**완료일**: 2025-11-20

#### 구현 완료 내용

**SessionContext 시스템** ✅
- 5분 타임아웃 기반 최근 업로드 문서 자동 추적
- 개인 DB 업로드 시 세션 자동 기록
- 시간 기반 문서 만료 관리
- 15개 단위 테스트 PASS

**IntentDetector 시스템** ✅
- 한국어/영어 문서 참조 패턴 감지
- 지시대명사: "이 문서", "그 파일" (confidence 0.7-0.95)
- 시간 기반: "방금 올린", "아까 업로드한"
- 파일명 직접 언급: confidence=1.0
- 23개 단위 테스트 PASS

**5단계 검색 우선순위** ✅
1. File Mention (@파일명) - 기존
2. Intent Detection (파일명 명시) - 신규, confidence=1.0
3. Session Context (참조 패턴 + 5분 이내) - 신규, confidence≥0.7
4. Auto Session (relevance≥0.7, 5분 이내) - 신규
5. Full DB (fallback) - 기존

#### 해결된 문제
**Before**: 격리 환경(63개)에서는 완벽 작동, 프로덕션(963개)에서는 신규 문서 검색 안 됨

**After**: SessionContext 도입으로 대규모 DB에서도 최근 업로드 문서 우선 검색

**사용자 경험 개선**:
- Before: "이 문서 요약해줘" → ❌ 전체 DB 검색
- After: "이 문서 요약해줘" → ✅ Intent 감지 → 세션 문서 우선 검색
- Before: "방금 올린 파일" → ❌ @파일명 필수
- After: "방금 올린 파일" → ✅ 시간 기반 참조 감지 → 자동 검색

**테스트 완료**:
- test_session_context.py (15 tests) ✅
- test_intent_detector.py (23 tests) ✅
- test_session_integration.py (3 scenarios) ✅
- test_regression_suite.py (8 regression tests) ✅

**문서**:
- TEST_PHASE3.5_GUI.md (GUI 테스트 가이드)
- PHASE3.5_TEST_RESULTS.md (테스트 결과 보고서)
- COMPREHENSIVE_TEST_PLAN.md (종합 테스트 계획)
- COMPREHENSIVE_TEST_RESULTS.md (49/49 통과 결과)

---

## 🚀 향후 우선순위 개발 계획

### 🎯 Phase A-3: Answer Verification 개선 (즉시 착수 추천) ⭐⭐⭐⭐⭐
**목표**: 재생성 빈도 50% 감소 및 응답 시간 단축
**예상 소요 시간**: 2-3일
**우선순위**: ★★★★★ (즉시 착수)
**참고 문서**: [docs/unified_improvement_roadmap.md](docs/unified_improvement_roadmap.md:38-139)

#### 배경
**현재 문제**:
```
Query: "kFRET 값은?"
1차 답변: "정보를 찾을 수 없습니다" (금지 구문)
→ 검증 실패
→ 2차 재생성 (+10-15초)
→ 성공

재생성 빈도: ~20% (5개 중 1개)
```

#### 구현 내용

**1. Prompt Engineering 강화** (30분)
- 금지 표현 명시화: "정보를 찾을 수 없습니다", "문서에 없습니다", "확인할 수 없습니다"
- 권장 표현 제시: "제공된 문서에 따르면...", "문서 #1의 5페이지에서..."
- NotebookLM 스타일 답변 예시 추가

**예상 효과**: 재생성 빈도 20% → 15% (-25%)

**2. Self-Consistency Check** (2일)
- N번 독립적 답변 생성 (temperature=0.5)
- 답변 간 일관성 점수 계산 (Jaccard similarity)
- 일관성 기반 최종 답변 선택 또는 통합

**예상 효과**: 재생성 빈도 15% → 10% (-33%), 답변 신뢰도 +15-20%

#### 예상 성과
| 지표 | Before | After | 개선 |
|------|--------|-------|------|
| 재생성 빈도 | 20% | 10% | -50% |
| 응답 시간 | 91.1초 | 76-81초 | -10-15초 |
| 정확도 | 기준치 | +5-8% | +5-8% |
| LLM 호출 비용 | 기준치 | -10% | -10% |

**리스크**: 낮음 (기존 시스템 확장)

---

### ⚡ Phase B-1: Qwen3-Embedding-8B 통합 (성능 대폭 향상) ⭐⭐⭐⭐⭐
**목표**: 차세대 임베딩 모델로 검색 정확도 대폭 향상
**예상 소요 시간**: 2일 (코드) + 4-6시간 (재임베딩)
**우선순위**: ★★★★☆ (Phase A-3 이후)
**참고 문서**: [docs/unified_improvement_roadmap.md](docs/unified_improvement_roadmap.md:142-273)

#### Qwen3-Embedding-8B 특징
| 특성 | mxbai-embed-large | Qwen3-Embedding-8B | 개선 |
|------|-------------------|---------------------|------|
| 모델 크기 | ~335M params | 8B params | +24배 |
| 임베딩 차원 | 1024D (고정) | 1024D/2048D/4096D (가변) | 유연성 |
| MTEB 점수 | ~56 | 70.58 | +26% |
| 다국어 지원 | 100+ languages | 100+ languages | 동일 |
| 특수 기능 | - | Matryoshka, Instruction-aware | 고급 |
| VRAM 요구량 | ~2GB | ~16GB | - |

#### 구현 계획
1. VectorStore 수정 (1일)
   - embedding_dimension 파라미터 추가 (2048D 권장)
   - Qwen3 전용 설정 검증
2. ChromaDB 재구축 (4-6시간)
   - 기존 DB 백업
   - 전체 문서 재임베딩
   - 인덱스 재구축
3. 차원 선택 전략
   - **2048D (권장)**: 정확도와 속도 균형
   - 4096D: 정확도 최우선
   - 1024D: 속도 최우선

#### 예상 성과
| 지표 | Before | After | 개선 |
|------|--------|-------|------|
| 검색 정확도 | 기준치 | +10-15% | +10-15% |
| MTEB 점수 | ~56 | ~70.58 | +26% |
| 응답 시간 | 76-81초 | 71-76초 | -5초 |
| 한국어 처리 | 보통 | 우수 | +20% |

**주의사항**: VRAM 16GB 이상 필요
**리스크**: 중간 (재임베딩 시간, VRAM 요구량)

---

### ⚡ Phase B-2: Matryoshka 차원 최적화 (속도 향상) ⭐⭐⭐⭐
**목표**: 다단계 검색으로 속도 30-50% 향상
**예상 소요 시간**: 3일
**우선순위**: ★★★★☆ (Phase B-1 이후)
**참고 문서**: [docs/unified_improvement_roadmap.md](docs/unified_improvement_roadmap.md:276-387)
**선행 조건**: Phase B-1 (Qwen3) 완료 필수

#### Matryoshka Representation Learning (MRL) 원리
```
기존 방식 (Single-dimension):
Query → 2048D 임베딩 → 전체 DB 검색 (느림)

Matryoshka 방식 (Multi-stage):
Query → 1024D (빠른 필터링, 100개 후보)
      → 2048D (중간 Re-ranking, 20개 후보)
      → 4096D (정밀 최종 선택, 5개 결과)

결과: 속도 30-50% 향상, 정확도 유지
```

#### 예상 성과
| 지표 | Before | After | 개선 |
|------|--------|-------|------|
| 검색 속도 | 15-20초 | 8-12초 | -30-50% |
| 정확도 | 기준치 | +5-10% | +5-10% |
| 메모리 사용량 | 100% | 70% | -30% |

**리스크**: 중간 (Ollama dimension 파라미터 지원 여부 확인 필요)

---

### ⚡ Phase B-3: Instruction-Aware Embedding (정확도 향상) ⭐⭐⭐
**목표**: 태스크별 최적화로 정확도 1-5% 추가 향상
**예상 소요 시간**: 1.5일
**우선순위**: ★★★☆☆ (Phase B-2 이후)
**참고 문서**: [docs/unified_improvement_roadmap.md](docs/unified_improvement_roadmap.md:390-482)
**선행 조건**: Phase B-1 (Qwen3) 완료 필수

#### Instruction-Aware Embedding 원리
```python
# 기존 방식
Embedding("TADF란 무엇인가?")

# Instruction-Aware
Embedding("Represent this query for retrieving relevant documents: TADF란 무엇인가?")
```

**효과**: 검색 의도 명확화, +1-5% 정확도

#### 예상 성과
| 지표 | Before | After | 개선 |
|------|--------|-------|------|
| 검색 정확도 | 기준치 | +3-5% | +3-5% |
| Citation 정확도 | 80% | 85-88% | +5-8% |
| 응답 시간 | 8-12초 | 6-10초 | -2초 |

**리스크**: 낮음 (기존 시스템 확장)

---

### 🎓 Self-RAG 메커니즘 완전 도입 ⭐⭐⭐⭐
**목표**: Self-reflection 및 Self-critique로 답변 품질 향상
**예상 소요 시간**: 2-3주
**우선순위**: ★★★★☆ (Phase A-3 또는 B-1 이후)
**참고 문서**: [docs/prompt_improvement_roadmap.md](docs/prompt_improvement_roadmap.md:86-113)
**참고 논문**: Self-RAG (Asai et al., 2023)

#### 구현 내용
```python
6단계 [Self-Reflection]:
   - 답변의 완전성을 평가하세요 (0-10점)
   - 답변의 정확성을 평가하세요 (0-10점)
   - 답변의 명확성을 평가하세요 (0-10점)
   - 점수가 7점 미만인 경우, 부족한 부분을 보완하세요

7단계 [Self-Critique]:
   - 답변에서 추측이나 일반 지식이 포함되었는지 확인
   - 문서에 없는 정보가 포함되었는지 확인
   - 출처 정보가 정확한지 확인
   - 문제가 발견되면 해당 부분을 수정
```

#### 예상 성과
- 할루시네이션: -40%
- 답변 품질: +20%
- 신뢰도 점수 제공

**리스크**: 중간 (구현 복잡도)

---

### 🎓 Corrective RAG 구현 ⭐⭐⭐⭐
**목표**: 오류 자동 감지 및 재생성 메커니즘
**예상 소요 시간**: 3-4주
**우선순위**: ★★★★☆ (Self-RAG와 병렬 가능)
**참고 문서**: [docs/prompt_improvement_roadmap.md](docs/prompt_improvement_roadmap.md:116-143)
**참고 논문**: Corrective RAG (Shi et al., 2024)

#### 구현 내용
1. 오류 감지 메커니즘
   - 문서 근거 부족 감지
   - 논리적 모순 감지
   - 출처 정보 부족 감지
2. 자동 재생성 로직
   - 오류 발견 시 자동 재생성
   - 추가 문서 검색 (필요 시)
3. 오류 유형별 대응 전략
   - 사실 오류 → 재검색
   - 논리 오류 → 재구성
   - 출처 부족 → 추가 검색

#### 예상 성과
- 오류율: -40%
- 정확도: +25%

**리스크**: 중간 (구현 복잡도)

---

### 🔒 프롬프트 인젝션 보안 대응 ⭐⭐⭐⭐
**목표**: 보안 취약점 완화
**예상 소요 시간**: 2주
**우선순위**: ★★★☆☆ (Phase A-3와 병렬 가능)
**참고 문서**: [docs/prompt_improvement_roadmap.md](docs/prompt_improvement_roadmap.md:146-174)
**참고 자료**: OWASP LLM Top 10

#### 구현 내용
1. 역할 및 제한사항 명시
   - 모델의 역할 명확히 정의
   - 기능 범위 제한
2. 출력 형식 검증 절차
   - 예상 출력 형식 명시
   - 형식 검증 로직
   - 이상 패턴 감지
3. 사용자 입력 검증
   - 입력 정규화
   - 위험 패턴 감지
   - 안전 처리

#### 예상 성과
- 보안 취약점: -50%
- 시스템 신뢰도 향상

**리스크**: 낮음

---

### 🎯 통합 성능 예상치 (모든 Phase 완료 시)

| Phase | 정확도 | 응답 시간 | Citation Rate | 누적 효과 |
|-------|--------|----------|---------------|----------|
| **현재 (v0.4.0)** | 기준치 | 기준치 | 95% | - |
| + A-3 | +5-8% | -10-15초 | 95% | +5-8%, -10-15초 |
| + B-1 (Qwen3) | +10-15% | -5초 | 95% | +15-23%, -15-20초 |
| + B-2 (Matryoshka) | +5-10% | -8-10초 | 95% | +20-33%, -23-30초 |
| + B-3 (Instruction) | +3-5% | -2초 | 95% | +23-38%, -25-32초 |
| + Self-RAG | +20% | +3초 | 95% | +43-58%, -22-29초 |
| + Corrective RAG | +25% | +3초 | 95% | +68-83%, -19-26초 |
| **총합** | **+68-83%** | **-19-26초** | **95%** | **NotebookLM 수준 초과** |

---

### 🎯 추천 실행 순서

**Option A: 성능 최우선 (추천)**
```
Week 1-2:  Phase A-3 (Answer Verification) + 보안 대응 (병렬)
Week 3-4:  Phase B-1 (Qwen3 통합)
Week 5-7:  Self-RAG 구현
Week 8-11: Corrective RAG 구현
Week 12+:  Phase B-2/B-3 (속도 최적화)
```

**Option B: 빠른 개선 우선**
```
Week 1:    Phase A-3 (Answer Verification)
Week 2-3:  보안 대응
Week 4+:   사용자 피드백 기반 선택
```

**Option C: 에이전트 기능 우선 (장기 비전)**
```
Week 1-2:  Phase A-3 (안정화)
Week 3-10: Phase 5 (에이전트 기능)
```

---

### 🎯 Phase 4: 다양한 문서 형식 지원 확대 (2025 Q1-Q2)
**목표**: 논문 외 개발결과물 통합

#### 4.1 고급 엑셀 처리
- [ ] **복잡한 표 구조 이해**: 병합 셀, 다층 헤더
- [ ] **수식 및 계산 보존**: Excel 수식 그대로 저장
- [ ] **차트 데이터 추출**: 그래프 → 텍스트 변환
- [ ] **시트 간 관계 분석**: 참조 관계 파악

#### 4.2 Raw Data 지원
- [ ] **CSV/TSV 파서**: 대용량 데이터 청킹
- [ ] **JSON/YAML**: 구조화된 데이터 임베딩
- [ ] **Parquet**: 컬럼 기반 저장 형식
- [ ] **데이터 스키마 인식**: 자동 필드 감지

#### 4.3 코드 및 로그 파일
- [ ] **Python/C++/Java**: 코드 구조 파싱
- [ ] **로그 파일 분석**: 에러 패턴 인식
- [ ] **실험 결과 매핑**: 코드 ↔ 결과 연결

**예상 소요 시간**: 3-4주
**우선순위**: Medium (Phase 3.5 완료 후)

---

### 🤖 Phase 5: 에이전트 기능 추가 (2025 Q2)
**목표**: 단순 QA → 자율 작업 수행

#### 5.1 도구 사용 (Tool Use)
- [ ] **검색 에이전트**: 특정 조건 문서 자동 검색
- [ ] **계산 에이전트**: 수식 계산, 단위 변환
- [ ] **시각화 에이전트**: 데이터 → 차트 자동 생성
- [ ] **파일 관리**: 문서 정리, 분류, 태깅

#### 5.2 다단계 추론 (Multi-Step Reasoning)
- [ ] **Plan & Execute**: 복잡한 질문 분해 → 단계별 실행
- [ ] **Self-Reflection**: 답변 검증 및 개선
- [ ] **Iterative Refinement**: 피드백 기반 재검색

#### 5.3 외부 도구 통합
- [ ] **Python REPL**: 실시간 코드 실행
- [ ] **데이터 분석**: pandas, numpy 자동 활용
- [ ] **API 호출**: 외부 서비스 연동 (필요 시)

**예상 소요 시간**: 6-8주
**우선순위**: Medium

---

### 👥 Phase 6: 협업 기능 (2025 Q3)
**목표**: 개인 사용 → 팀 협업

#### 6.1 공유 및 권한 관리
- [ ] **팀 DB 공유**: 부서별 공유 DB
- [ ] **권한 레벨**: Read/Write/Admin
- [ ] **액세스 로그**: 사용 기록 추적

#### 6.2 피드백 및 개선
- [ ] **답변 평가**: 👍/👎 피드백
- [ ] **오류 보고**: 잘못된 정보 신고
- [ ] **개선 제안**: 커뮤니티 기여

#### 6.3 지식 큐레이션
- [ ] **태그 시스템**: 문서 분류 체계
- [ ] **즐겨찾기**: 자주 찾는 문서
- [ ] **추천 시스템**: 관련 문서 자동 추천

**예상 소요 시간**: 4-6주
**우선순위**: Low

---

### 📊 Phase 7: 고급 분석 및 인사이트 (2025 Q4)
**목표**: 정보 제공 → 인사이트 발견

#### 7.1 트렌드 분석
- [ ] **연구 동향**: 시간별 키워드 변화
- [ ] **기술 로드맵**: 발전 방향 예측
- [ ] **Gap 분석**: 부족한 연구 영역 식별

#### 7.2 지식 그래프
- [ ] **논문 간 관계**: 인용 네트워크
- [ ] **개념 연결**: 키워드 클러스터링
- [ ] **영향력 분석**: 핵심 논문 식별

#### 7.3 자동 보고서 생성
- [ ] **주간 요약**: 새로운 문서 요약
- [ ] **프로젝트 리포트**: 관련 자료 자동 수집
- [ ] **벤치마크**: 경쟁사 분석

**예상 소요 시간**: 8-10주
**우선순위**: Future

---

## 🧭 개발 가이드라인

### 🎨 코드 스타일
- **Python Style**: PEP 8 준수
- **Docstring**: Google 스타일
- **Type Hints**: 필수 (Python 3.10+)
- **Logging**: logging 모듈 사용 (print 금지)

### 🏗️ 아키텍처 원칙
1. **모듈화**: 각 기능을 독립적 모듈로 분리
2. **의존성 주입**: 하드코딩 최소화
3. **설정 중심**: config.json으로 모든 동작 제어
4. **오프라인 우선**: 외부 네트워크 의존성 최소화
5. **테스트 가능**: 단위 테스트 작성 용이하게

### 🔒 보안 원칙
- **API Key 보호**: `.env` 파일 또는 환경 변수 (config.json 금지)
- **입력 검증**: 사용자 입력 sanitization
- **로그 보안**: 민감 정보 마스킹
- **권한 최소화**: 필요한 권한만 요청

### 📝 커밋 규칙
```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type**:
- `feat`: 새로운 기능
- `fix`: 버그 수정
- `docs`: 문서 변경
- `refactor`: 리팩토링
- `test`: 테스트 추가/수정
- `chore`: 빌드/설정 변경

**예시**:
```
feat(rag_chain): Add exhaustive retrieval mode

- Detect "모든/전체" keywords
- Increase max_results to 100
- Add classifier integration

Closes #42
```

---

## 🧪 테스트 철학 및 전략

### 🎯 테스트 철학 (2025-01-20 회고 기반)

#### 핵심 원칙: "작동" vs "쓸모"

**❌ 잘못된 성공 기준:**
- 에러 없이 실행됨
- 청크가 생성됨
- DB에 저장됨
- 쿼리가 실행됨

**✅ 올바른 성공 기준:**
- **사용자가 업로드한 PDF로부터 답변이 나오는가?**
- **사용자의 문제가 실제로 해결되는가?**
- **시스템이 의도한 대로 동작하는가?**

#### 회고에서 배운 교훈

**문제 상황 (2025-01-20):**
```python
# 이전 테스트 - 거짓 양성(False Positive)
✓ PDF 처리: 21개 청크 생성
✓ 벡터 DB 저장: 21/21 성공
✓ RAG 쿼리 실행: 에러 없음

결과: "테스트 통과!" 라고 보고

실제 상황:
✗ 사용자가 업로드한 PDF는 검색 결과에 없음
✗ 시스템이 다른 문서들로 답변함
✗ 실제로는 사용 불가능한 상태
```

**교훈:**
1. **개별 컴포넌트 테스트만으로는 부족**
   - 각 단계가 성공해도 전체 시스템이 실패할 수 있음
   - "저장됨" ≠ "검색 가능함"

2. **End-to-end 사용자 시나리오 필수**
   - 실제 사용자가 하는 행동을 그대로 테스트
   - 업로드 → 질문 → 그 문서로부터 답변 확인

3. **격리된 테스트 환경 필요**
   - 프로덕션 DB(963개 문서)에서는 신규 문서가 묻힘
   - 테스트 전용 DB로 간섭 제거

4. **실제 검색 가능성 검증**
   - 메타데이터 존재 확인 ≠ 검색 가능
   - 직접 similarity_search()로 확인

### 📋 필수 테스트 체크리스트

#### ✅ 모든 신규 기능은 다음을 포함해야 함:

1. **User Scenario Test (최우선)**
   ```python
   def test_real_user_scenario():
       # 1. 사용자 행동: 문서 업로드
       upload_document(pdf_path)

       # 2. 사용자 행동: 질문
       answer = query("이 문서에 대해 설명해줘")

       # 3. 검증: 해당 문서가 소스에 있는가?
       assert uploaded_doc_id in answer.sources

       # 4. 검증: 답변이 의미있는가?
       assert len(answer.text) > 100
   ```

2. **Isolation Test (격리 검증)**
   - 테스트 전용 DB 사용
   - 다른 문서의 간섭 제거
   - 순수하게 해당 기능만 테스트

3. **Direct Verification (직접 확인)**
   ```python
   # 메타데이터 확인만으로는 부족
   # 실제 검색 테스트 필수
   results = vectorstore.similarity_search(query)
   assert our_document_id in [r.metadata['document_id'] for r in results]
   ```

4. **Negative Test (실패 케이스)**
   - 대규모 DB 환경에서도 테스트
   - 경쟁 문서가 많을 때도 찾을 수 있는가?

### 🧪 테스트 전략

### 1️⃣ 단위 테스트 (Component Test)
```bash
# 개별 모듈 테스트
python -m pytest tests/unit/

# 예: 청킹 엔진만 테스트
python test_pdf_chunking_only.py
```

**목적**: 개별 기능이 에러 없이 작동하는지 확인
**한계**: 전체 시스템의 유용성은 보장 못 함

### 2️⃣ 통합 테스트 (Integration Test)
```bash
# Phase 2 통합 테스트
python test_phase2_integration.py

# Phase 2 검증 테스트
python test_phase2_verification.py
```

**목적**: 여러 모듈이 함께 작동하는지 확인
**한계**: 격리 환경이므로 실제 환경의 문제를 못 찾을 수 있음

### 3️⃣ End-to-End 테스트 (User Scenario Test) ⭐ 최우선
```bash
# 실제 사용자 시나리오 테스트
python test_REAL_user_scenario.py

# Phase 3 실제 검색 가능성 테스트
python test_phase3_searchability.py
```

**목적**: 실제 사용자가 시스템을 사용할 때 문제가 없는지 확인
**검증 항목**:
- ✅ 업로드한 PDF가 검색되는가?
- ✅ 그 PDF로부터 답변이 생성되는가?
- ✅ 답변이 인용 출처에 포함되는가?

**예시**:
```python
# test_REAL_user_scenario.py (2025-01-20)
# 격리된 테스트 DB 사용
test_db = VectorStoreManager(persist_directory="data/test_chroma_db")

# 사용자 시나리오
upload_pdf("my_document.pdf")
answer = query("What is this document about?")

# 검증: 우리 PDF가 소스에 있는가?
assert "my_document.pdf" in str(answer.sources)
```

### 4️⃣ 프로덕션 환경 테스트
```bash
# 대규모 DB 환경 테스트
python test_production_scale.py

# 동시 사용자 시뮬레이션
python test_concurrent_users.py
```

**목적**: 실제 운영 환경에서의 문제 발견
**주의**: 격리 테스트에서는 통과했지만 프로덕션에서 실패할 수 있음
- 예: 963개 문서 환경에서 신규 문서 검색 우선순위 문제

### 5️⃣ 빌드 검증
```bash
# 빌드 후 실행 테스트
dist/RAG_System_v3.6.0/RAG_System_v3.6.0.exe
```

### ⚠️ 테스트 안티패턴 (하지 말 것)

1. **"에러 없음" = "성공"으로 착각**
   ```python
   # ❌ 잘못된 테스트
   def test_pdf_upload():
       chunks = process_pdf(pdf_path)
       assert len(chunks) > 0  # 청크만 생성되면 통과

   # ✅ 올바른 테스트
   def test_pdf_upload_and_searchable():
       chunks = process_pdf(pdf_path)
       store_chunks(chunks)

       # 실제로 검색되는가?
       results = search(chunks[0].content[:50])
       assert any(c.id in str(r) for c in chunks for r in results)
   ```

2. **메타데이터 존재 확인만으로 만족**
   ```python
   # ❌ 잘못된 검증
   metadata = db.get_metadata(doc_id)
   assert metadata is not None  # 메타데이터만 있으면 통과

   # ✅ 올바른 검증
   search_results = db.similarity_search(query)
   assert doc_id in [r.metadata['document_id'] for r in search_results]
   ```

3. **격리 환경에서만 테스트**
   - 격리 환경: 통과 ✓
   - 프로덕션 환경: 실패 ✗
   - 반드시 두 환경 모두 테스트!

### 📊 테스트 우선순위

1. **Tier 1: User Scenario Test** (필수, 매 PR)
   - 실제 사용자 관점
   - 격리 환경 + 프로덕션 환경

2. **Tier 2: Integration Test** (필수, 매 PR)
   - 주요 기능 통합
   - 회귀 방지

3. **Tier 3: Unit Test** (권장)
   - 개별 모듈
   - 리팩토링 시 유용

4. **Tier 4: Performance Test** (주기적)
   - 응답 시간
   - 메모리 사용량

---

## 🚀 빌드 및 배포

### PyInstaller 빌드
```bash
# 빌드 실행
venv/Scripts/python.exe -m PyInstaller -y \
  --name=RAG_System_v3.6.0 \
  --icon=oc.ico \
  --onedir \
  --console \
  --add-data="resources;resources" \
  --add-data="config.json.example;." \
  --add-data="models;models" \
  --hidden-import=win32timezone \
  --hidden-import=sentencepiece \
  --hidden-import=chromadb.api.segment \
  --hidden-import=chromadb.api.types \
  --hidden-import=chromadb.segment.impl.vector.local_persistent_hnsw \
  --collect-all=chromadb \
  --exclude-module=magic \
  desktop_app.py

# 빌드 결과 확인
dir dist\RAG_System_v3.6.0
```

### 배포 체크리스트
- [ ] config.json.example 업데이트
- [ ] README.md 버전 정보 업데이트
- [ ] CHANGELOG 작성
- [ ] 테스트 통과 확인
- [ ] 빌드 성공 확인
- [ ] 실행 파일 동작 확인
- [ ] Git 태그 생성 (v3.6.0)
- [ ] Release 노트 작성

---

## 📚 주요 문서

### 사용자 문서
- [README.md](README.md) - 설치 및 사용 가이드
- [설정 가이드](docs/CONFIGURATION.md) - 상세 설정 방법
- [FAQ](docs/FAQ.md) - 자주 묻는 질문

### 개발자 문서
- [SYSTEM_QC_REPORT.md](SYSTEM_QC_REPORT.md) - 품질 검증 보고서
- [Phase 2 완료 요약](docs/PHASE2_COMPLETION_SUMMARY.md)
- [질문 분류기 가이드](docs/QUESTION_CLASSIFIER_GUIDE.md)
- [Phase D 구현 계획](docs/phase_d_implementation_plan.md)

### 기술 문서
- [RAG_Pipeline_Architecture.md](RAG_Pipeline_Architecture.md) - **RAG 파이프라인 아키텍처 (Pipeline A & B)**
- [아키텍처 다이어그램](docs/ARCHITECTURE.md)
- [API 레퍼런스](docs/API_REFERENCE.md)
- [성능 벤치마크](docs/BENCHMARKS.md)

---

## 🤝 기여 방법

### 이슈 제보
1. GitHub Issues에서 이슈 검색
2. 중복 이슈 없으면 새 이슈 생성
3. 템플릿에 따라 상세 정보 기입

### 코드 기여
1. Fork 후 feature 브랜치 생성
2. 코드 작성 및 테스트
3. Pull Request 생성
4. 리뷰 반영 후 머지

### 문서 기여
- 오타 수정: 즉시 PR
- 내용 추가: Issue 논의 후 PR
- 번역: 별도 브랜치에서 작업

---

## 📞 문의 및 지원

- **GitHub Issues**: 버그 제보 및 기능 요청
- **Discussions**: 질문 및 토론
- **Email**: [내부 메일 주소]

---

## 📄 라이선스

이 프로젝트는 내부 사용 목적으로 제작되었습니다.
외부 공개 시 라이선스 정책을 별도로 정의할 예정입니다.

---

## 🙏 감사의 말

이 프로젝트는 다음 오픈소스 프로젝트들을 기반으로 합니다:
- [LangChain](https://github.com/langchain-ai/langchain) - RAG 프레임워크
- [ChromaDB](https://github.com/chroma-core/chroma) - 벡터 데이터베이스
- [PySide6](https://wiki.qt.io/Qt_for_Python) - Qt6 Python 바인딩
- [sentence-transformers](https://github.com/UKPLab/sentence-transformers) - Re-ranker

---

## 📋 개발 히스토리

### v0.4.5 (2025-11-26)
**테마**: 파일 멘션 동적 업데이트 및 아키텍처 문서화

**주요 변경사항**:

1. **파일 멘션 (@) 동적 업데이트** ✅
   - 파일 업로드 후 즉시 멘션 목록 갱신 (프로그램 재시작 불필요)
   - 라디오 버튼 선택(통합/개인/공유 DB)에 따라 멘션 목록 동적 변경
   - 구현 상세:
     - `input_widget` 별칭 추가로 문서 업로드 후 자동 갱신
     - `_get_current_db_type()` 메서드로 현재 선택된 DB 타입 감지
     - `get_filenames_only()`에 `db_type` 파라미터 전달
     - 라디오 버튼 `toggled` 시그널 연결하여 즉시 갱신
   - 수정 파일: `ui/chat_widget.py`, `utils/vector_store.py`

2. **RAG 파이프라인 아키텍처 문서 작성** ✅
   - `RAG_Pipeline_Architecture.md` 신규 작성 (905 lines)
   - Pipeline A: 문서 수집 (Document Ingestion) 전체 워크플로우 문서화
   - Pipeline B: 질의응답 (Question Answering) 프로세스 재구성
   - Vision 임베딩 시스템 상세 설명:
     - 이미지 변환 (pdf2image) → Vision LLM 분석 → 텍스트 설명 → 임베딩 → 벡터화
     - Hybrid Vision Mode 및 API 설정 포함
   - 컴포넌트 분석, 최적화 포인트, 성능 메트릭 추가

3. **프로젝트 문서 업데이트** ✅
   - `.CLAUDE.md` 업데이트:
     - 현재 버전 v0.4.5로 갱신
     - 프로젝트 구조에 `RAG_Pipeline_Architecture.md` 추가
     - 주요 문서 섹션에 아키텍처 문서 링크 추가
   - `README.md` 업데이트 예정

**Git Commits**:
- `f98cbaf`: chore: Release v0.4.5 - File mention dynamic updates
- `fa12751`: fix: File mention (@) updates dynamically with DB selection
- `2f8e94e`: docs: Add comprehensive Pipeline A documentation for vision embedding system

**소요 시간**: 파일 멘션 기능 (2시간) + 아키텍처 문서화 (3시간) = 총 5시간

---

### v0.4.1 (2025-01-23)
**테마**: UX 개선 및 공유 DB 안정화

**주요 변경사항**:

1. **UI/UX 개선** ✅
   - 업로드 취소 버튼 추가: 진행 중 언제든 취소 가능
   - 상세 업로드 진행 메시지: 파일 타입 아이콘 (📄 PDF, 📊 PPTX, 📈 XLSX, 📝 TXT) + 단계별 피드백
   - 채팅 입력창 힌트: "메시지 입력 (Shift+Enter: 줄바꿈, @: 파일 언급)"
   - 파일 자동완성 개선: Tab/Enter로 선택, Esc로 취소
   - 사이드바 접기/펼치기 버튼 위치 최적화 (대화창 상단 검색 범위 라인)
   - 설정 UI 그룹화: LLM 설정 / 임베딩 설정 / Vision API 설정 구분
   - 중복 Vision 설정 필드 제거 ("전송 방식 (레거시)" 삭제)
   - 수정 파일: `ui/document_widget.py`, `ui/chat_widget.py`, `ui/settings_widget.py`, `ui/main_window.py`

2. **공유 DB 안정화** ✅
   - 공유 DB 재접속 로직 개선:
     - 설정 저장 시: 경로 설정되면 항상 재접속 시도 → UI 즉시 반영
     - 메뉴 재접속 시: 성공/실패 여부와 관계없이 UI 업데이트
     - DB 파일 미존재 시 자동 비활성화
   - MainWindow 참조 보존 수정:
     - 문제: `self.parent()`가 QTabWidget을 반환 (addTab 후 reparenting)
     - 해결: `__init__`에서 `self.main_window = parent` 저장
     - UI 업데이트 신뢰성 100% 달성
   - 수정 파일:
     - `ui/settings_widget.py` (lines 15, 320-367)
     - `ui/main_window.py` (lines 506-509)
     - `utils/vector_store.py` (lines 1528-1530)

3. **프로그램 내부 사용법 업데이트** ✅
   - 메뉴 → 도움말 → 사용방법 다이얼로그 업데이트:
     - 검색 범위 선택 (통합/개인/공유) 안내 추가
     - 업로드 취소 버튼 기능 설명
     - 공용 DB 재접속 메뉴 안내
     - Shift+Enter 줄바꿈 팁 추가
     - 사이드바 접기/펼치기 기능 안내
   - 수정 파일: `ui/main_window.py` (lines 423-489)

**Git Commits**:
- `3a6d23b`: refactor: Fix tab width and relocate control buttons to search mode row
- `a0bdad6`: fix: Update shared DB UI on every settings save when enabled
- `88f1ef3`: fix: Correct shared DB connection and UI update logic
- `649f44c`: Revert "refactor: Settings save only updates config, no reconnect"
- `91bbc56`: fix: Fix settings UI update by storing MainWindow reference
- `a5abe76`: fix: File autocomplete now selects correct item with Tab/Enter
- `6c80964`: fix: Fix upload cancel and file autocomplete UX issues
- `2061224`: feat: Improve UX with 5 major enhancements

**소요 시간**: UI 개선 (6시간) + 공유 DB 로직 (4시간) = 총 10시간

---

### v0.4.0 (2025-01-20)
**테마**: Phase 3.5 완료 - SessionContext + Intent Detection 통합 (Major Feature Release)

**주요 변경사항**:

1. **SessionContext 구현** ✅
   - 최근 업로드 문서 자동 추적 (5분 타임아웃)
   - 개인 DB 업로드 시 세션 기록
   - 시간 기반 문서 만료 관리
   - 15개 단위 테스트 PASS
   - 구현 파일: `utils/session_context.py` (169 lines)

2. **IntentDetector 구현** ✅
   - 문서 참조 패턴 감지 (한국어/영어)
   - 지시대명사: "이 문서", "그 파일" (신뢰도 0.7-0.95)
   - 시간 기반: "방금 올린", "아까 업로드한"
   - 파일명 직접 언급: confidence=1.0
   - 23개 단위 테스트 PASS
   - 구현 파일: `utils/intent_detector.py` (245 lines)

3. **RAG Chain 검색 우선순위 통합** ✅
   - 검색 우선순위 적용:
     1. File Mention (@파일명) - 기존
     2. Intent Detection (파일명 명시) - 신규, confidence=1.0
     3. Session Context (참조 패턴 + 5분 이내) - 신규, confidence≥0.7
     4. Auto Session (relevance≥0.7, 5분 이내) - 신규
     5. Full DB (fallback) - 기존
   - Relevance threshold 기반 필터링 (0.7)
   - 수정 파일: `utils/rag_chain.py` (lines 155-170, 1590-1649, 3212-3315)

4. **GUI 통합 완료** ✅
   - `desktop_app.py`: SessionContext 초기화 및 전달
   - `ui/main_window.py`: session_context 중계
   - `ui/document_widget.py`: 업로드 시 자동 세션 기록
   - 로그 메시지: "📌 Session 추적 활성화: [파일명]"

5. **버그 수정** ✅
   - `_last_retrieved_docs` 튜플 형식 오류 (line 3304)
   - `_generate_multi_queries` 존재하지 않는 메서드 (line 3235)

6. **통합 테스트 완료** ✅
   - Scenario 1 (Intent Detection - 지시대명사): PASS
   - Scenario 2 (Time-based Reference): PASS
   - Scenario 3 (Auto Session Context): PASS
   - 총 41개 테스트 통과 (38 unit + 3 integration)

**사용자 경험 개선**:
- Before: "이 문서 요약해줘" → ❌ 전체 DB 검색
- After: "이 문서 요약해줘" → ✅ Intent 감지 → 세션 문서 우선 검색
- Before: "방금 올린 파일" → ❌ @파일명 필수
- After: "방금 올린 파일" → ✅ 시간 기반 참조 감지 → 자동 검색

**설정 추가**:
```json
{
  "enable_session_priority": true,
  "session_relevance_threshold": 0.7
}
```

**테스트 파일**:
- `test_session_context.py` (15 tests)
- `test_intent_detector.py` (23 tests)
- `test_session_integration.py` (3 scenarios)
- `test_gui_import.py` (import 검증)

**문서 추가**:
- `TEST_PHASE3.5_GUI.md` (GUI 테스트 가이드)
- `PHASE3.5_TEST_RESULTS.md` (테스트 결과 보고서)

**원칙 준수**:
- "작동" + "쓸모" 원칙 만족
- 의미적으로 유용한 답변 생성 검증
- Production-ready 상태 달성

---

### v3.6.3 (2025-01-20)
**테마**: Phase 3 완료 및 테스트 철학 수립 - "작동" vs "쓸모" 구분

**주요 변경사항**:

1. **Phase 3 PDF Vision Hybrid Mode 완료** ✅
   - Smart Vision Decision: 이미지/표 있는 페이지만 Vision API 사용
   - Poppler 자동 감지 및 번들링
   - PDF → 이미지 변환 (pdf2image)
   - Vision API 통합 (OpenAI/Ollama)
   - 비용 최적화: 33-70% Vision API 호출 감소
   - 구현 파일: `utils/pdf_chunking_engine.py` (lines 998-1252)

2. **실제 사용자 시나리오 테스트 완료**
   - `test_REAL_user_scenario.py` 작성
   - 격리 환경 테스트: 완벽 성공 (63개 문서)
   - 프로덕션 환경 문제 발견: 신규 문서 검색 우선순위 이슈 (963개 문서)
   - 검증 결과:
     - ✅ PDF 처리: 21개 청크 (Vision 14 + Text 7)
     - ✅ 벡터 저장: 21/21 성공
     - ✅ 직접 검색: 상위 3개 결과에 포함
     - ✅ RAG 쿼리: 3개 페이지(8, 10, 4)를 소스로 사용
     - ✅ 최종: ACTUALLY USEFUL [PASS]

3. **테스트 철학 재정립** (회고 기반)
   - **핵심 원칙 수립**: "작동" vs "쓸모" 구분
   - 잘못된 기준: 에러 없음, 청크 생성, DB 저장
   - 올바른 기준: 사용자 PDF로부터 답변 생성, 문제 해결, 의도대로 동작
   - 교훈:
     1. 개별 컴포넌트 테스트 부족 ("저장됨" ≠ "검색 가능함")
     2. End-to-end 사용자 시나리오 필수
     3. 격리 환경 + 프로덕션 환경 모두 테스트
     4. 실제 검색 가능성 직접 확인

4. **필수 테스트 체크리스트 추가**
   - User Scenario Test (최우선)
   - Isolation Test (격리 검증)
   - Direct Verification (직접 확인)
   - Negative Test (실패 케이스)

5. **테스트 안티패턴 문서화**
   - "에러 없음" = "성공" 착각
   - 메타데이터 존재만 확인
   - 격리 환경에서만 테스트

6. **Phase 3.5 계획 수립**: 대규모 DB 신규 문서 우선순위 개선
   - Option A: 시간 기반 부스팅 (추천, 1일)
   - Option B: 문서별 검색 필터 (2-3일)
   - Option C: 개인 DB 우선 검색 (4-5일)

**발견된 문제**:
- 격리 환경(63개): 완벽 작동 ✅
- 프로덕션(963개): 신규 문서가 기존 문서에 밀려 검색 안 됨 ✗

**원인 분석**:
1. 신규 문서 relevance score < 기존 문서
2. 문서별 필터링 없음
3. 최신 업로드 우선순위 부스팅 없음

**테스트 파일**:
- `test_REAL_user_scenario.py` (새 파일)
- `test_phase3_rag_FIXED.py` (수정)
- `real_user_test_FINAL.txt` (결과)
- `PHASE3_TEST_RESULTS.md` (기존 - 거짓 양성)

**문서 업데이트**:
- `.CLAUDE.md` 테스트 철학 섹션 추가 (lines 561-774)
- Phase 3 완료 상태 반영
- Phase 3.5 계획 추가 (신규 문서 우선순위)
- Phase 번호 재조정 (4→4, 5→6, 6→7)

**영향**:
- Phase 3 핵심 기능: ✅ 완료
- 남은 과제: 대규모 DB 최적화 (Phase 3.5)
- 테스트 방법론: 근본적 개선

---

### v3.6.2 (2025-01-09)
**테마**: 메타데이터 검색 아키텍처 분석 및 Phase 2.5 로드맵 업데이트

**주요 변경사항**:
1. **상용 RAG 서비스 벤치마크 분석**
   - Semantic Scholar, Perplexity, Elicit, ChatGPT Enterprise 아키텍처 분석
   - Hybrid Search, Query Decomposition, Knowledge Graph 전략 연구
   - `docs/METADATA_SEARCH_ANALYSIS.md` 작성

2. **Phase 2.5.6: 메타데이터 기반 검색 로드맵 추가**
   - PDF 메타데이터 자동 추출 파이프라인 (GROBID/PyPDF2)
   - ChromaDB 메타데이터 필드 확장 (authors, title, journal, year, doi)
   - Question Classifier에 `metadata_search` 타입 추가
   - 2단계 검색 전략 (메타데이터 필터 → 전체 청크 로드 → 요약)

3. **구현 우선순위 정의**:
   - Phase 1 (1-2일): 파일명 기반 검색 (즉시 적용 가능)
   - Phase 2 (3-4일): PyPDF2 기반 메타데이터 추출
   - Phase 3 (선택): GROBID 통합 (90% 정확도)

**배경**:
- 사용자 요구사항: "김철수 저자의 논문 요약" 같은 메타데이터 쿼리 지원 필요
- 현재 시스템: 벡터 검색만으로는 메타데이터 정확 매칭 불가
- 상용 서비스: Metadata Index + Vector Index 병렬 운영

**영향**:
- 코드 변경 없음 (분석 및 문서화만)
- 향후 구현 시 DB 재구축 필요 (메타데이터 필드 추가)

---

### v3.6.1 (2025-01-09)
**테마**: 벡터 DB 최적화 및 임베딩 모델 유연성 강화

**주요 변경사항**:
1. **ChromaDB 거리 함수 cosine 변경**
   - 정규화된 임베딩 모델에 최적화 (mxbai-embed-large, qwen3-embedding-8B)
   - config.json에 `chroma_distance_function: "cosine"` 추가
   - VectorStoreManager에 distance_function 파라미터 추가

2. **공유 DB 볼륨 레이블 수정**
   - LGDKBB → LGDKRB (정확한 볼륨 레이블)
   - drive_scanner.py 및 main_window.py 에러 메시지 수정

3. **qwen3-embedding-8B 테스트 스크립트 추가**
   - check_qwen3_embedding.py 생성
   - 차원(4096d), 정규화(L2=1.0), 속도 검증
   - Ollama/OpenAI 호환 API 모두 지원

**영향**:
- DB 재구축 필요 (거리 함수 변경)
- mxbai ↔ qwen3 모델 전환 시 DB 재구축 필요 (차원 불일치: 1024 vs 4096)

---

**마지막 업데이트**: 2025-11-26
**작성자**: Claude Code + OC Papers Team
**버전**: v0.4.5
