# 키워드/고유명사 검색 개선 테스트 계획

## 구현 완료 사항

### P0: 키워드/고유명사 감지 및 BM25 가중치 증가 ✅
- `_detect_keyword_query()` 메서드 추가
- `_question_type_params`에 "keyword" 타입 추가 (BM25 0.8, threshold 0.4)
- `_search_candidates()`에서 키워드 감지 통합

### P1: Score Threshold 동적 조정 ✅
- 키워드 질문일 때 adaptive threshold 자동 적용 (0.4)
- 최소 개수 보장 로직 강화 (threshold 0.2로 완화)

### P2: 검색 범위 확대 ✅
- 키워드 질문일 때 `initial_k` 2배 증가

### P3: 폴백 검색 강화 ✅
- `_try_bm25_fallback()` 메서드 추가
- 키워드 질문에서 결과 부족 시 BM25 단독 검색으로 재시도

### P4: 검색 결과 검증 ✅
- `_validate_search_results()` 메서드 추가
- 키워드 매칭 사후 검증 및 로깅

---

## 테스트 시나리오

### 1. 기본 키워드 감지 테스트

#### 1.1 저자명 검색 (한글 질문)
**질문**: "Changmin Keum 이 사람이 저자인 논문 찾아줘"

**예상 동작**:
- [KEYWORD] 키워드 질문 감지 로그 출력
- BM25 가중치 0.8로 증가 확인
- 검색 범위 확대 (initial_k 2배)
- 검색 성공 (이전에는 실패했음)

**검증 포인트**:
- 키워드 감지 로그 확인
- BM25 가중치 로그 확인
- 검색 결과에 "Changmin Keum" 포함 문서 존재
- 최종 답변에 저자명 및 논문 정보 포함

#### 1.2 저자명 검색 (영어 질문)
**질문**: "Find papers by Changmin Keum"

**예상 동작**:
- 영어 패턴 감지 ("by ~")
- BM25 가중치 증가
- 검색 성공

**검증 포인트**:
- 영어 패턴 감지 로그 확인
- 검색 결과 정확도

#### 1.3 저자명 검색 (인용부호 포함)
**질문**: "저자 'Changmin Keum'의 논문"

**예상 동작**:
- 인용부호 패턴 감지
- 키워드 추출: "Changmin Keum"
- 검색 성공

**검증 포인트**:
- 인용부호 내 키워드 추출 확인

### 2. 고유명사 감지 테스트

#### 2.1 대문자 고유명사
**질문**: "John Smith 논문 찾아줘"

**예상 동작**:
- 고유명사 패턴 감지 (대문자 시작)
- 키워드: "John Smith"
- BM25 가중치 증가

**검증 포인트**:
- 고유명사 감지 로그
- 키워드 추출 정확도

#### 2.2 복합 고유명사
**질문**: "University of St Andrews 논문"

**예상 동작**:
- 복합 고유명사 감지
- 키워드: "University of St Andrews"
- 검색 성공

**검증 포인트**:
- 복합 고유명사 전체 추출 확인

### 3. Score Threshold 동적 조정 테스트

#### 3.1 낮은 점수 문서 포함 확인
**질문**: "Changmin Keum 저자 논문"

**예상 동작**:
- 키워드 질문 감지
- adaptive_threshold_percentile: 0.4 적용
- 낮은 점수 문서도 포함 (이전에는 필터링됨)

**검증 포인트**:
- [SCORE] 동적 Threshold 로그 확인 (0.4 적용)
- 검색 결과 개수 증가
- 낮은 점수 문서 포함 여부

#### 3.2 최소 개수 보장
**질문**: "존재하지 않는 저자명 XYZ 검색"

**예상 동작**:
- 키워드 질문 감지
- 검색 결과 부족 시 threshold 0.2로 완화
- 최소 3개 문서 보장 시도

**검증 포인트**:
- 최소 개수 보장 로그 확인
- threshold 완화 로그 확인

### 4. 검색 범위 확대 테스트

#### 4.1 initial_k 확대 확인
**질문**: "Changmin Keum 논문"

**예상 동작**:
- 키워드 질문 감지
- initial_k 2배 증가 (예: 20 → 40, 60 → 120)
- 더 많은 후보 문서 검색

**검증 포인트**:
- [KEYWORD] 검색 범위 확대 로그 확인
- initial_k 값 확인
- 검색 결과 개수 증가

### 5. 폴백 검색 강화 테스트 (P3)

#### 5.1 BM25 폴백 작동
**질문**: "Changmin Keum 저자 논문" (벡터 검색 실패 시나리오)

**예상 동작**:
- 벡터 검색 결과 부족 (< min_num_results)
- [FALLBACK] BM25 단독 검색 시도
- BM25 결과 추가

**검증 포인트**:
- [FALLBACK] 로그 확인
- BM25 검색 성공 로그
- 결과 개수 증가 확인

#### 5.2 비키워드 질문에서 폴백 비활성화
**질문**: "OLED 효율은?"

**예상 동작**:
- 키워드 질문 아님
- 폴백 검색 비활성화
- 일반 검색 진행

**검증 포인트**:
- [FALLBACK] 로그 없음
- 일반 검색 로그만 확인

### 6. 검색 결과 검증 테스트 (P4)

#### 6.1 키워드 매칭 성공
**질문**: "Changmin Keum 논문"

**예상 동작**:
- 검색 결과에서 "Changmin Keum" 키워드 매칭 확인
- [VALIDATE] ✓ 모든 검색 결과에 키워드 포함 로그

**검증 포인트**:
- [VALIDATE] 로그 확인
- 매칭된 문서 비율 확인

#### 6.2 키워드 매칭 실패 경고
**질문**: "존재하지 않는 저자명 검색"

**예상 동작**:
- 검색 결과에 키워드 없음
- [VALIDATE] ⚠ 키워드 매칭 실패 로그
- 원본 결과 반환 (최소 개수 보장)

**검증 포인트**:
- 경고 로그 확인
- 매칭 실패 비율 확인

#### 6.3 부분 매칭 처리
**질문**: "Changmin Keum 논문" (일부 문서에만 키워드 포함)

**예상 동작**:
- 매칭된 문서 우선 반환
- 매칭 안 된 문서는 최소 개수 보장용으로만 포함

**검증 포인트**:
- 매칭된 문서가 상위에 위치
- 매칭 비율 로그 확인

---

## 통합 테스트 시나리오

### 시나리오 1: 실제 사용 사례
**질문**: "Changmin Keum 이 사람이 저자인 논문 찾아줘"

**전체 플로우**:
1. 질문 번역: "Find papers authored by Changmin Keum"
2. 키워드 감지: "Changmin Keum" 추출
3. BM25 가중치 증가: 0.5 → 0.8
4. 검색 범위 확대: initial_k 2배
5. 검색 수행: Hybrid Search (BM25 0.8 + Vector 0.2)
6. Score filtering: threshold 0.4 적용
7. 결과 부족 시: BM25 폴백 검색
8. 결과 검증: 키워드 매칭 확인
9. 최종 답변 생성

**검증 포인트**:
- 각 단계별 로그 확인
- 최종 답변에 저자명 및 논문 정보 포함
- "찾을 수 없습니다" 응답 없음

### 시나리오 2: 영어 직접 입력
**질문**: "Find papers by Changmin Keum"

**전체 플로우**:
1. 번역 불필요 (이미 영어)
2. 키워드 감지: "Changmin Keum" 추출
3. BM25 가중치 증가
4. 검색 성공

**검증 포인트**:
- 영어 패턴 감지 확인
- 검색 성공률

### 시나리오 3: 복합 고유명사
**질문**: "University of St Andrews 소속 저자 논문"

**전체 플로우**:
1. 고유명사 감지: "University of St Andrews"
2. 키워드 추출
3. 검색 성공

**검증 포인트**:
- 복합 고유명사 전체 추출
- 검색 정확도

---

## 성능 측정

### 메트릭
1. **키워드 감지 정확도**: 감지된 키워드 질문 비율
2. **검색 성공률**: 키워드 질문에서 검색 성공 비율
3. **검색 시간**: 키워드 질문 검색 시간 (범위 확대 영향)
4. **BM25 폴백 사용률**: 폴백 검색이 트리거된 비율
5. **검증 실패율**: 키워드 매칭 실패 비율

### 기준값
- **이전**: 저자명 검색 성공률 ~30%
- **목표**: 저자명 검색 성공률 ~70%+
- **검색 시간**: 2배 확대로 인한 시간 증가 < 50%
- **폴백 사용률**: < 20% (대부분 정상 검색)

---

## 로그 확인 체크리스트

### 필수 로그
- [ ] `[KEYWORD] 키워드 질문 감지` - 키워드 감지 성공
- [ ] `[KEYWORD] 검색 범위 확대` - initial_k 확대
- [ ] `[SEARCH] BM25=0.8, Vector=0.2` - BM25 가중치 증가
- [ ] `[SCORE] 동적 Threshold: ... (질문 유형: keyword)` - Threshold 조정
- [ ] `[FALLBACK]` - 폴백 검색 (결과 부족 시)
- [ ] `[VALIDATE]` - 검색 결과 검증

### 선택적 로그
- [ ] `[SCORE] 최소 개수 보장` - Threshold 완화
- [ ] `[FALLBACK] BM25 검색 성공` - BM25 폴백 성공
- [ ] `[VALIDATE] ⚠ 키워드 매칭 실패` - 검증 실패 경고

---

## 테스트 실행 방법

### 1. 단위 테스트
```python
# 키워드 감지 테스트
from utils.rag_chain import RAGChain

rag_chain = RAGChain(...)
result = rag_chain._detect_keyword_query(
    "Find papers by Changmin Keum",
    "Changmin Keum 이 사람이 저자인 논문 찾아줘"
)
assert result['is_keyword'] == True
assert 'Changmin Keum' in result['keywords']
```

### 2. 통합 테스트
```python
# 전체 검색 플로우 테스트
response = rag_chain.query("Changmin Keum 이 사람이 저자인 논문 찾아줘")
# 로그 확인 및 결과 검증
```

### 3. 실제 사용 테스트
- Desktop App에서 직접 질문 입력
- 로그 파일 확인
- 검색 결과 및 답변 확인

---

## 예상 개선 효과

### 정량적 개선
- 저자명 검색 성공률: 30% → 70%+
- False Negative 감소: 50% 감소
- 관련 문서 누락률: 30% 감소
- 검색 실패 복구율: 40% 향상

### 정성적 개선
- 사용자 경험 향상: "찾을 수 없습니다" 응답 감소
- 검색 정확도 향상: 키워드 매칭 강화
- 안정성 향상: 폴백 검색으로 복구 가능

---

## 주의사항

1. **성능 모니터링**: 검색 범위 확대로 인한 시간 증가 확인
2. **BM25 준비 상태**: BM25가 준비되지 않으면 폴백 검색 실패
3. **번역 품질**: 번역 오류로 인한 키워드 손실 가능성
4. **로그 레벨**: 상세 로그로 디버깅 가능하도록 설정

---

## 다음 단계

1. **테스트 실행**: 위 시나리오 순차 실행
2. **로그 분석**: 각 단계별 로그 확인
3. **성능 측정**: 메트릭 수집 및 분석
4. **개선 사항**: 필요 시 파라미터 조정
5. **사용자 피드백**: 실제 사용 사례 수집





